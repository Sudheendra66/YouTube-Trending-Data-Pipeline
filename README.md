# YouTube-Trending-Data-Pipeline
A fully cloud-native ETL pipeline that pulls live YouTube trending data across **10 regions**, runs it through a **Medallion Architecture** (Bronze → Silver → Gold), enforces data quality gates, and delivers analytics-ready tables — all orchestrated end-to-end on AWS.
